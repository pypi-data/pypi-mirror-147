[tool.poetry]
name = "rate-exchange-fetcher"
version = "0.0.1"
description = ""
authors = ["Yann Rabiller <yann.rabiller@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.27.1"

[tool.poetry.dev-dependencies]
pytest = "^7.1.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
