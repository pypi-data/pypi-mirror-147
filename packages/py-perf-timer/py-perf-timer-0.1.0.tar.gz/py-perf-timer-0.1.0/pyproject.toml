[tool.poetry]
name = "py-perf-timer"
version = "0.1.0"
description = ""
authors = ["Mordechai Alter <motcke1@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

packages = [
    { include = "py_perf_timer" }
]